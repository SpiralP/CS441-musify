{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "cd packages/server && npm run start",
    "build": "lerna run build",
    "dev": "nodemon --ext ts,tsx,json --exec \"npm run build && npm run start\"",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "nodemon": "^2.0.2"
  }
}
